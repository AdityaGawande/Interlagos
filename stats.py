# Make a function which takes a default named csv, runs min (+percentage away from mean), max (+percentage away from mean), mean, SD on its first column.
# Name of the CSV and number of rows in the csv are arguments to the function
# Print the resulting values into the terminal
# Later - add values into google sheet+log